import {getResponsiveSize} from '@utils/responsive';

export const borderWidth = {
  xs: getResponsiveSize(0.5),
  sm: getResponsiveSize(1),
  md: getResponsiveSize(1.5),
  lg: getResponsiveSize(2),
  xl: getResponsiveSize(2.5),
};
