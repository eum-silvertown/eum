spring:
  cloud:
    config:
      uri: http://127.0.0.1:8888  # Config 서버 URI
      name: eum
  profiles:
    active: user

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/  # Eureka 서버 URI
    registerWithEureka: true
    fetchRegistry: true