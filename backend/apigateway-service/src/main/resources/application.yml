server:
  port: 8000

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      # TODO : ?? Service URL? ????
      defaultZone: http://localhost:8761/eureka

spring:
  application:
    name: apigateway-service
  cloud:
    gateway:
      mvc:
        routes:
          # TODO : Micro Service ??
management:
  endpoints:
    web:
      exposure:
        include: "*"